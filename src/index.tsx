// ⚠️ DEAD CODE — leftover from the Create React App → Vite migration (v0.6.0).
//
// This file is NOT used by the Vite build. Vite uses `src/main.tsx` as
// the React entry point (imported by the root `index.html`). This
// placeholder is intentionally tracked so the file can be discovered
// and removed by a maintainer via:
//
//     git rm src/index.tsx
//
// See CHANGELOG.md ([Unreleased] / Removed) and TODO.md for context.
export {};
