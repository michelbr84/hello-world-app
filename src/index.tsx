// DEPRECATED: This file is a leftover from the Create React App era.
// Vite's HTML entry (`index.html` at the repo root) loads
// `src/main.tsx` as the ES module entry point, not this file.
// This stub is kept temporarily so the removal is reviewable.
// Delete the file entirely in a follow-up commit.
export {};
