// 🪠 DEAD CODE — leftover from the Create React App → Vite migration (v0.6.0).
//
// This file is NOT used by the Vite test setup. The active test setup is
// `src/test-setup.ts`, registered via `setupFiles` in `vite.config.ts`.
// This placeholder is intentionally tracked so the file can be discovered
// and removed by a maintainer via:
//
//      git rm src/setupTests.ts
//
// See CHANGELOG.md ([Unreleased] / Removed) and TODO.md for context.
export {};
