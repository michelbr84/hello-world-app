- [ ] Remove the dead Create React App artifacts (`public/index.html`, `src/index.tsx`, `src/setupTests.ts`) — *deferred from v0.6.0; the physical `git rm` is still pending on `master` (the files remain as zero-byte orphans). Tracked under `TODO.md` and `CHANGELOG.md` `[Unreleased] / Pending`.*